<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    android:layout_height="match_parent"
    tools:context=".Orientation">

    <TextView
        android:id="@+id/guidance"
        android:layout_width="300dp"
        android:layout_height="111dp"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:lineSpacingExtra="24sp"
        android:text="Adaptive Diagnosis Prototype"
        android:textColor="@android:color/background_dark"
        android:textSize="30sp"
        app:layout_constraintBottom_toTopOf="@+id/adaptiveButton"
        app:layout_constraintEnd_toEndOf="@+id/adaptiveButton"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toStartOf="@+id/adaptiveButton"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/adaptiveButton"
        android:layout_width="300dp"
        android:layout_height="49dp"
        android:background="@drawable/main_screen_buttons"
        android:text="Adaptive Diagnosis"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/guidance" />

    <Button
        android:id="@+id/recordingButton"
        android:layout_width="300dp"
        android:layout_height="49dp"
        android:layout_marginTop="28dp"
        android:background="@drawable/main_screen_buttons"
        android:text="Recording"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/adaptiveButton"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="@id/adaptiveButton"
        app:layout_constraintTop_toBottomOf="@+id/adaptiveButton" />

</androidx.constraintlayout.widget.ConstraintLayout>